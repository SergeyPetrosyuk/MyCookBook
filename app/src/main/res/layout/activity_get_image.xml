<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="40dp"
    tools:context="com.mlsdev.serhiy.mycookbook.ui.activity.GetImageActivity"
    android:background="#75000000"
    android:id="@+id/rl_container">

    <RelativeLayout
        android:id="@+id/rl_btns_holder"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:paddingBottom="15dp"
        android:paddingTop="15dp"
        android:background="@android:color/white">

        <Button
            android:id="@+id/btn_make_photo"
            style="@style/drawer_menu_btn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:drawableLeft="@mipmap/ic_action_camera"
            android:text="@string/make_photo"/>

        <Button
            android:id="@+id/btn_choose_in_gallery"
            android:layout_below="@id/btn_make_photo"
            style="@style/drawer_menu_btn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:drawableLeft="@mipmap/ic_action_picture"
            android:text="@string/choose_in_gallery"/>

        <View
            android:id="@+id/take_photo_divider"
            android:layout_below="@id/btn_choose_in_gallery"
            android:layout_margin="15dp"
            style="@style/divider" />

        <Button
            android:id="@+id/btn_take_photo_cancel"
            android:layout_below="@id/take_photo_divider"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/drawer_btn_bg"
            android:layout_alignParentRight="true"
            android:layout_marginRight="15dp"
            android:text="@android:string/cancel"
            android:paddingLeft="10dp"
            android:paddingRight="10dp"/>

        </RelativeLayout>

</RelativeLayout>
